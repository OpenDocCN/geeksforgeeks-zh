# Java 8 流

> 原文: [https://www.geeksforgeeks.org/java-8-stream/](https://www.geeksforgeeks.org/java-8-stream/)

[流介绍](https://www.geeksforgeeks.org/stream-in-java/)

## Java Intstream

*   `anyMatch()`
*   `noneMatch()`
*   `mapToLong()`
*   `findAny()`
*   `forEachOrdered()`
*   `forEach()`
*   `allMatch()`
*   `filter()`
*   `findFirst()`
*   `flatMapToInt()`
*   `mapToInt()`
*   `map()`
*   `peek()`
*   `count()`
*   `iterator()`
*   `generate()`
*   `skip()`
*   `summaryStatistics()`
*   `Builder()`
*   `empty()`
*   `toArray()`
*   带流过滤器的列表总和

*   `forEachOrdered()`
*   `mapToInt(UnaryOperator mapper)`
*   `flatMapToInt(IntFunction mapper)`
*   `forEach()`
*   `sum()`
*   `range()`
*   `sorted()`
*   `anyMatch()`
*   `concat()`
*   `filter()`
*   `findAny()`
*   `allMatch()`
*   `mapToDouble()`
*   `max()`
*   `min()`
*   `count()`
*   `average()`
*   `peek()`
*   `empty()`
*   `distinct()`
*   `parallel()`
*   `Builder()`
*   `boxed()`
*   `mapToDouble()`
*   `IntStream asDoubleStream()`
*   `limit()`
*   `mapToObj()`
*   `mapToLong()`
*   `IntStream asLongStream()`
*   `IntBinaryOperator`
*   `IntStream toArray()`
*   `mapToInt()`
*   `reduce()`

## Java Longstream

*   `findAny()`
*   `noneMatch()`
*   `min()`
*   `max()`
*   `map(LongUnaryOperator mapper)`
*   `iterator()`
*   `forEach()`
*   `forEachOrdered()`
*   `flatMapToLong(LongFunction mapper)`
*   `filter()`
*   `empty()`
*   `findFirst()`
*   `of()`
*   `anyMatch()`
*   `allMatch()`
*   `distinct()`
*   `concat()`
*   `average()`
*   `summaryStatistics()`
*   `sorted()`
*   `skip()`
*   `sequential()`
*   `range()`
*   `rangeClosed()`
*   `peek()`
*   `parallel()`
*   `generate()`
*   `sum()`
*   搜索元素
*   `limit()`
*   `mapToObj()`
*   `mapToDouble()`
*   `LongStream asDoubleStream()`
*   `reduce(LongBinaryOperator)`
*   `reduce()`
*   `LongStream toArray()`
*   `boxed()`
*   `Builder()`

## Java Doublestream

（注：原文中此部分未提供具体内容列表，故保留标题结构。）

*   `empty()`
*   `findFirst()`
*   `flatMap()`
*   `sorted()`
*   `forEachOrdered()`
*   `forEach()`
*   `generate()`
*   `summaryStatistics()`
*   `filter()`
*   `of()`
*   `average()`
*   `map(DoubleUnaryOperator mapper)`
*   `iterator()`
*   `max()`
*   `anyMatch()`
*   `min()`
*   `count()`
*   `skip()`
*   `distinct()`
*   `concat()`
*   `sequential()`
*   `peek()`
*   `findAny()`
*   `allMatch()`
*   `parallel()`
*   `sum()`
*   `noneMatch()`
*   `boxed()`
*   `mapToObj()`
*   `limit()`
*   `reduce(double identity, DoubleBinaryOperator op)`
*   `toArray()`

## 更多有用链接:

*   [最近关于 Java 8 流的文章](https://www.geeksforgeeks.org/tag/java-stream/)
*   [最近关于 Java 的文章](https://www.geeksforgeeks.org/category/java/)
*   [编码实践平台](https://practice.geeksforgeeks.org/)
*   [选择题](https://www.geeksforgeeks.org/java-multiple-choice-questions/)
*   [Java 类所有文章](https://www.geeksforgeeks.org/category/java/)