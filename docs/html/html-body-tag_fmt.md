
# HTML 标签

> 原文: [https://www.geeksforgeeks.org/html-body-tag/](https://www.geeksforgeeks.org/html-body-tag/)

HTML 中的 `<body>` 标签用于定义 HTML 页面内部呈现的主要内容。它总是包含在 `<html>` 标签中。`<body>` 标签是 `<html>` 标签的最后一个孩子。正文标签包含开始标签和结束标签。

## 语法

```html
<body> Body Contents... </body>
```

## 属性

在 `<body>` 标签中有很多属性是从 HTML5 中继承下来的，如下所示:

*   **背景**: 包含背景图片的 URL。用于设置背景图像。
*   **bgcolor**: 用于指定图像的背景颜色。
*   **alink**: 用于指定活动链接的颜色。
*   **链接**: 用于指定已访问链接的颜色。
*   **文本**: 它指定文档中文本的颜色。
*   **vlink**: 指定访问链接的颜色。

## 示例

### 使用 HTML 正文标签

所有的内容都放在主体标签里面。

```html
<!DOCTYPE html>
<html>

<!-- body tag starts here -->
<body>
    <h1>GeeksforGeeks</h1>
    <h2>body Tag</h2>

<p>This is paragraph text</p>

</body>
<!-- body tag ends here -->

</html>
```

### 展示 Body 标签的功能及其 CSS 实现的示例

```html
<!DOCTYPE html>
<html>

<!-- style on the body tag -->
<!-- body tag starts here -->
<body style="background-color:seagreen">
    <h1>GeeksforGeeks</h1>
    <h2>HTML body Tag</h2>

<p>This is paragraph Tag</p>

</body>
<!-- body tag ends here -->
</html>
```

## 支持的浏览器

*   谷歌 Chrome
*   微软公司出品的 web 浏览器
*   火狐浏览器
*   歌剧
*   旅行队
