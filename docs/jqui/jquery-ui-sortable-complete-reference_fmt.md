# jQuery UI 可排序完整参考

> 原文: [https://www.geeksforgeeks.org/jquery-ui-sortable-complete-reference/](https://www.geeksforgeeks.org/jquery-ui-sortable-complete-reference/)

`jQuery UI 可排序小部件`用于执行选举事件。这个小部件使用鼠标单独或成组地选择元素。这个小部件中有很多可用的选项、方法和事件，它们都在下面列出并分类。

## jQuery UI 可排序小部件选项

*   [jQuery UI 可排序 `appendTo` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-appendto-option/)
*   [jQuery UI 可排序 `axis` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-widget-axis-option/)
*   [jQuery UI 可排序 `cancel` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-cancel-option/)
*   [jQuery UI 可排序 `classes` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-widget-classes-option/)
*   `jQuery UI 可排序 connectWith option`
*   `可排序的 contain 选项`
*   [jQuery UI 可排序 `cursor` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-cursor-option/)
*   [jQuery UI 可排序 `cursorAt` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-cursorat-option/)
*   [jQuery UI 可排序 `delay` option](https://www.geeksforgeeks.org/jquery-ui-sortable-delay-option/)
*   [jQuery UI 可排序 `disabled` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-widget-disabled-option/)
*   `jQuery UI 可排序 `distance` 选项`
*   `jQuery UI 可排序 `dropOnEmpty` 选项`
*   `jQuery UI 可排序 `forceHelperSize` 选项`
*   `jQuery UI 可排序 `forcePlaceholderSize` option`
*   `jQuery UI 可排序 `grid` 选项`
*   `可排序 `handle` 选项`
*   `jQuery UI 可排序 `helper` 选项`
*   `可排序 `items` 选项`
*   [jQuery UI 可排序 `opacity` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-opacity-option/)
*   `可排序 `placeholder` 选项`
*   [jQuery UI 可排序 `revert` 选项](https://www.geeksforgeeks.org/jquery-ui-sortable-revert-option/)
*   `可排序 `scroll` 选项`
*   `可排序 `scrollSensitivity` 选项`
*   `jQuery UI 可排序 `scrollSpeed` 选项`
*   `可排序 `tolerance` 选项`
*   `jQuery UI 可排序 `zIndex` 选项`

## jQuery UI 可排序小部件方法

*   `jQuery UI 可排序 `cancel()` 方法`
*   `jQuery UI 可排序 `destroy()` 方法`
*   `jQuery UI 可排序 `disable()` 方法`
*   `jQuery UI 可排序 `enable()` 方法`
*   `jQuery UI 可排序 `instance()` 方法`
*   `jQuery UI 可排序 `option()` 方法`
*   `jQuery UI 可排序 `refresh()` 方法`
*   `jQuery UI 可排序 `refreshPositions()` 方法`
*   `jQuery UI 可排序 `serialize()` 方法`
*   `jQuery UI 可排序 `toArray()` 方法`
*   `jQuery UI 可排序 `widget()` 方法`

## jQuery UI 可排序小部件事件

*   `可排序的 `activate` 事件`
*   `jQuery UI 可排序 `beforeStop` 事件`
*   `可排序的 `change` 事件`
*   `可排序的 `create` 事件`
*   `可排序的 `deactivate` 事件`
*   `可排序的 `out` 事件`
*   `jQuery UI 可排序 `over` 事件`
*   `可排序的 `receive` 事件`
*   `可排序的 `remove` 事件`
*   `jQuery UI 可排序 `sort` 事件`
*   `可排序的 `start` 事件`
*   `可排序的 `stop` 事件`
*   `可排序的 `update` 事件`