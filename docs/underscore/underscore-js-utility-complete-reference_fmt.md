# 下划线.js 实用程序完整参考

> 原文：`https://www.geeksforgeeks.org/underscore-js-utility-complete-reference/`

## 下划线.js `_.noConflict()` 功能

## 下划线.js `_.identity()` 功能

## 下划线.js `_.constant()` 功能

## 下划线.js `_.noop()` 功能

## 下划线.js `_.times()` 功能

## 下划线.js `_.random()` 功能

## 下划线.js `_.mixin()` 功能

## 下划线.js `_.iteratee()` 函数

## 下划线.js `_.uniqueId()` 功能

## 下划线.js `_.escape()` 功能

## 下划线.js `_.unescape()` 功能

## 下划线.js `_.result()` 函数

## 下划线.js `_.now()` 功能

## 下划线.js `_.template()` 功能